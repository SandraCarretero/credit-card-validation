import styled from 'styled-components';
import {
	StyledInput,
	StyledInputNameContainer,
	StyledLabel
} from '../InputName/input-name.styles';

const StyledInputNumberContainer = styled(StyledInputNameContainer)``;
const StyledLabelNumber = styled(StyledLabel)``;
const StyledInputNumber = styled(StyledInput)``;

export { StyledInputNumberContainer, StyledLabelNumber, StyledInputNumber };
