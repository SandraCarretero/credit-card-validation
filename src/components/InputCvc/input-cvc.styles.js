import styled from 'styled-components';
import {
	StyledInput,
	StyledInputNameContainer,
	StyledLabel
} from '../InputName/input-name.styles';

const StyledInputCvcContainer = styled(StyledInputNameContainer)`
	width: 50%;
`;
const StyledLabelCvc = styled(StyledLabel)``;
const StyledInputCvc = styled(StyledInput)``;

export { StyledInputCvcContainer, StyledLabelCvc, StyledInputCvc };
